# PostgreSQL settings
spring.datasource.url=jdbc:postgresql://db:5432/oauth2db
spring.datasource.username=admin
spring.datasource.password=secretpassword
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate settings
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Static resources
spring.mvc.static-path-pattern=/static/**
spring.resources.static-locations=classpath:/META-INF/resources/,classpath:/resources/,classpath:/static/,classpath:/public/

# ChatGPT API (attento a non lasciare la chiave esposta in produzione!)
chatgpt.api.endpoint=https://api.chatgpt.com/v1/completions
chatgpt.api.key=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
# Configurazione Keycloak
#keycloak.realm=fascicolo-digitale
#keycloak.auth-server-url=http://${KEYCLOAK_HOST:localhost:8090}
#keycloak.ssl-required=external
#keycloak.resource=fascicolo-digitale-backend
#keycloak.credentials.secret=0LQ7QFS5ceSv42sTtkSj0U3uMSEKnyrD
#keycloak.use-resource-role-mappings=true
#keycloak.bearer-only=true

# Abilita Spring Security
#spring.security.enabled=false

